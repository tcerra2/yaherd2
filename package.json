{
  "name": "yolo-tracking-app",
  "version": "1.0.0",
  "description": "Client-side YOLO object tracking web app",
  "main": "server.js",
  "scripts": {
    "postinstall": "python3 -m pip install --no-cache-dir --extra-index-url https://download.pytorch.org/whl/cpu -r requirements-web.txt || python -m pip install --no-cache-dir --extra-index-url https://download.pytorch.org/whl/cpu -r requirements-web.txt",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "keywords": ["yolo", "tracking", "tensorflow", "object-detection"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}
